/**
 * Version of the shared contract vocabulary in this package.
 *
 * Every persisted or transmitted document produced against these schemas
 * carries an explicit `schemaVersion`/`schema_version` literal so consumers
 * can reject unknown versions instead of guessing.
 */
export const CONTRACTS_SCHEMA_VERSION = 1;
